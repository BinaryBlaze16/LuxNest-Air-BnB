{
  "name": "luxnest-luxury-platform",
  "version": "2.0.0",
  "description": "LuxNest - Luxury Vacation Rental & Airbnb Clone Platform",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "render-build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "postinstall": "npm --prefix server install",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "server": "npm --prefix server start",
    "client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
